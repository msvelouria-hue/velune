
export default {
  expo: {
    // Do not place OpenAI, Vuxo, PhotoRoom, or other provider secrets here.
    // Values in Expo config can be bundled into the client app. Server-side
    // provider keys belong in Firebase Functions secrets instead.
    extra: {},
  },
};
